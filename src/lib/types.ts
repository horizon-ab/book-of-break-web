export interface Move {
    name: string;
    description: string;
    tags: string[];
    
}

export interface MoveWithId {
    id: string;
    name: string;
    description: string;
    tags: string[];
    
}

export interface User {

}